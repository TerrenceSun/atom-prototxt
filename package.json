{
  "name": "language-prototxt",
  "main": "./lib/language-prototxt",
  "version": "0.0.2",
  "description": "highlight and quick navigation supports for .prototxt file, the model definition file of Caffe",
  "keywords": [
    "prototxt",
    "highlight"
  ],
  "repository": "https://github.com/TerrenceSun/atom-prototxt.git",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "atom-space-pen-views": "^2.0.0",
    "event-kit": "^1.0.0"
  },
  "configSchema": {
    "autoReactivate": {
      "type": "boolean",
      "default": true,
      "title": "Keep last state",
      "description": "Check to reactive guidline window according to last activate status."
    },
    "active": {
      "type": "boolean",
      "default": "false",
      "title": "Last opened",
      "description": "The last activate status. The value is managed automatically."
    }
  }
}
